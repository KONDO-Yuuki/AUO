{
  "name": "auo-mock-server",
  "version": "0.0.2",
  "description": "mock-server for auo-collection",
  "main": "dist/index.js",
  "scripts": {
    "build": "../../node_modules/.bin/babel src -d dist --source-maps inline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KONDO-Yuuki",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "auo-interfaces": "^0.0.2"
  }
}
