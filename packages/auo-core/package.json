{
  "name": "auo-core",
  "version": "0.0.1",
  "description": "core of auo project",
  "main": "dist/index.js",
  "scripts": {
    "build": "../../node_modules/.bin/babel src -d dist --source-maps inline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KONDO-Yuuki",
  "license": "MIT",
  "devDependencies": {
    "auo-interfaces": "^0.0.1"
  }
}
